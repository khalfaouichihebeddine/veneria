import { api } from '@/api';
const cell = {padding:'16px 20px',borderBottom:'1px solid var(--line)'};
export default async function AdminProducts() { const products=await api.getProducts(); return <main className="container" style={{padding:'60px 0'}}><span className="eyebrow">Administration</span><h1 style={{fontSize:42,margin:'12px 0 30px'}}>Produits</h1><div className="card" style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse',textAlign:'left'}}><thead><tr><th style={cell}>Nom</th><th style={cell}>Prix</th><th style={cell}>Statut</th></tr></thead><tbody>{products.map((product)=><tr key={product.id}><td style={cell}>{product.name}</td><td style={cell}>{product.price} TND</td><td style={cell}>Publie</td></tr>)}</tbody></table></div></main>; }
