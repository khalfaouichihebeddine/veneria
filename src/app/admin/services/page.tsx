import { api } from '@/api';
export default async function AdminServices() { const services=await api.getServices(); return <main className="container" style={{padding:'60px 0'}}><span className="eyebrow">Administration</span><h1 style={{fontSize:42,margin:'12px 0 30px'}}>Services</h1><div className="grid">{services.map((service)=><div className="card" style={{padding:22}} key={service.id}><span className="eyebrow">{service.duration}</span><h2 style={{fontSize:22}}>{service.name}</h2><p style={{color:'var(--muted)'}}>{service.priceFrom} EUR · Publie</p></div>)}</div></main>; }
