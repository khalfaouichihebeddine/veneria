'use client'; import Link from 'next/link'; import { useState } from 'react'; export default function Login(){ const [error,setError]=useState(''); return <main className="container" style={{padding:'100px 0',maxWidth:520}}><span className="eyebrow">Espace prive</span><h1 style={{fontSize:46,margin:'14px 0'}}>Se connecter</h1><form className="card" style={{padding:26,display:'grid',gap:16}} onSubmit={(e)=>{e.preventDefault();setError('Connexion locale activee.')}}><label>Email<input required type="email" defaultValue="camille.martin@example.com" style={field}/></label><label>Mot de passe<input required type="password" defaultValue="password" style={field}/></label>{error&&<p style={{color:'var(--green)'}}>{error}</p>}<button style={{background:'var(--green)',color:'white',border:0,padding:14,fontWeight:700}}>Ouvrir ma session</button><Link href="/create-account" style={{color:'var(--green)',textAlign:'center'}}>Creer un compte</Link></form></main> } const field={display:'block',width:'100%',marginTop:7,padding:12,border:'1px solid var(--line)',borderRadius:6,background:'var(--cream)'};
