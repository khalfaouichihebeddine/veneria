import Image from 'next/image'; import Link from 'next/link'; import { notFound } from 'next/navigation'; import { api } from '@/api';
export default async function ProductDetail({params}:{params:Promise<{slug:string}>}){ const {slug}=await params; const product=await api.getProduct(slug); if(!product) notFound(); return <main className="container" style={{padding:'70px 0'}}><Link href="/produits" style={{color:'var(--muted)',fontSize:14}}>← Retour a la collection</Link><div style={{display:'grid',gridTemplateColumns:'1.05fr .95fr',gap:60,marginTop:30,alignItems:'center'}}>{product.imageUrl&&<Image src={product.imageUrl} alt={product.name} width={900} height={800} style={{width:'100%',height:550,objectFit:'cover',borderRadius:10}}/>}<div><span className="eyebrow">Piece signature</span><h1 style={{fontSize:56,lineHeight:1,margin:'18px 0'}}>{product.name}</h1><p style={{fontSize:19,lineHeight:1.7,color:'var(--muted)'}}>{product.description}</p><div style={{display:'flex',justifyContent:'space-between',borderTop:'1px solid var(--line)',borderBottom:'1px solid var(--line)',padding:'18px 0',margin:'30px 0'}}><span>Prix</span><strong style={{color:'var(--ochre)'}}>{product.price} EUR</strong></div><Link href="/contact" style={{display:'inline-block',background:'var(--green)',color:'white',padding:'15px 22px',fontWeight:700}}>Demander cette piece ↗</Link></div></div></main> }
