import { api } from '@/api'; import { ProductCard } from '@/components/product-card';
export default async function Products(){ const products=await api.getProducts(); return <main className="container" style={{padding:'78px 0'}}><span className="eyebrow">La collection</span><h1 style={{fontSize:52,margin:'14px 0 12px'}}>Objets choisis</h1><p style={{color:'var(--muted)',maxWidth:530,lineHeight:1.7,marginBottom:40}}>Des formes utiles, des matieres qui vieillissent bien et des pieces qui trouvent leur place.</p><div className="grid">{products.map((product)=><ProductCard key={product.id} product={product}/>)}</div></main> }
