import Link from 'next/link'; import { notFound } from 'next/navigation'; import { api } from '@/api';
export default async function ServiceDetail({params}:{params:Promise<{slug:string}>}){ const {slug}=await params; const service=await api.getService(slug); if(!service) notFound(); return <main className="container" style={{padding:'92px 0',maxWidth:900}}><span className="eyebrow">Accompagnement Veneria</span><h1 style={{fontSize:60,lineHeight:1.05,margin:'18px 0'}}>{service.name}</h1><p style={{fontSize:21,color:'var(--muted)',lineHeight:1.7,maxWidth:650}}>{service.description}</p><div className="card" style={{marginTop:42,padding:26,display:'flex',justifyContent:'space-between',alignItems:'center'}}><span>{service.duration}</span><strong style={{color:'var(--ochre)'}}>A partir de {service.priceFrom} EUR</strong></div><Link href="/contact" style={{display:'inline-block',background:'var(--green)',color:'white',padding:'15px 22px',fontWeight:700,marginTop:28}}>Parler de votre projet ↗</Link></main> }
