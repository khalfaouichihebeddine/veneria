import { api } from '@/api'; import { ServiceCard } from '@/components/service-card';
export default async function Services(){ const services=await api.getServices(); return <main className="container" style={{padding:'78px 0'}}><span className="eyebrow">Notre regard</span><h1 style={{fontSize:52,margin:'14px 0 12px'}}>Des espaces qui vous ressemblent.</h1><p style={{color:'var(--muted)',maxWidth:580,lineHeight:1.7,marginBottom:40}}>De la premiere conversation a la derniere piece posee, nous composons avec vous des lieux justes et vivants.</p><div className="grid">{services.map((service)=><ServiceCard key={service.id} service={service}/>)}</div></main> }
